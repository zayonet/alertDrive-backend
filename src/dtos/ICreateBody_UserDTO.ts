export default interface CreateBody_UserDTO {
  heights: string;
  weigh: string;
  blood_type: string;
  body_pressure_min: string;
  body_pressure_max: string;
  glicemia: string;
  user_id: string;
}